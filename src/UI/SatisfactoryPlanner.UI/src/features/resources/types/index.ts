export type Resource = {
    id: string;
    name: string;
    extractedResources: number;
    totalResources: number;
};

export type ResourceDetails = {
    id: string;
    code: string;
    name: string;
    description: string;
};
