export type Resource = {
    id: string;
    name: string;
    extractionRate: number;
    maxExtractionRate: number;
};

export type ResourceDetails = {
    id: string;
    code: string;
    name: string;
    description: string;
};
